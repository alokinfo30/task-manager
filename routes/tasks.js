const express = require('express');
const router = express.Router();
const Task = require('../models/Task');

// Implement CRUD operations for tasks (GET, POST, PUT, DELETE)
// Using async/await and error handling

module.exports = router;
